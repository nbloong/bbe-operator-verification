name: Verify current operator assets
on:
  push:
    branches:
      - clean-rebuild-staging-20260812
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diagnose and clean LM006 certificate in test workspace
        run: python .github/repair_lm006.py
      - name: LM001
        if: always()
        run: python .github/verify_one.py BBE-LM-001
      - name: LM002
        if: always()
        run: python .github/verify_one.py BBE-LM-002
      - name: LM003
        if: always()
        run: python .github/verify_one.py BBE-LM-003
      - name: LM004
        if: always()
        run: python .github/verify_one.py BBE-LM-004
      - name: LM005
        if: always()
        run: python .github/verify_one.py BBE-LM-005
      - name: LM006
        if: always()
        run: python .github/verify_one.py BBE-LM-006
      - name: LM007
        if: always()
        run: python .github/verify_one.py BBE-LM-007
